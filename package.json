{
  "name": "ilios-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend/rea-investment-fe && npm install && cd ../../backend/ilios-server && pip install -r requirements.txt"
  }
}
