Native AlsoEnergy V2 credential testing is now working end-to-end in dev/control testing. A real user successfully tested credentials and the provider account flipped to Verified. The native adapter obtains an access token directly from api.alsoenergy.com; no old GCP Cloud Function path is used.

The remaining production blocker is durable credential storage.

Production currently blocks telemetry credential Save/Test routes unless durable storage is configured. We need to configure and verify production durable storage so internal users can test AlsoEnergy credentials in production without credentials being lost on restart/redeploy.

Do not implement Sync Sites customer enablement.
Do not implement KMC native adapter.
Do not implement QuickBooks.
Do not touch auth except smoke validation.
Do not migrate JWT storage.
Do not expose credentials, provider tokens, service account JSON, passwords, or API keys.
Do not print secrets.

Objective

Enable and verify durable telemetry V2 credential storage in production, then confirm production internal users can save and test AlsoEnergy credentials through the native adapter.

Current confirmed state
Native AlsoEnergy adapter exists:
app.integrations.telemetry.native_also_energy_adapter.NativeAlsoEnergyAdapter
Alembic migration:
ff21_telemetry_v2_native_ae
Dev catalog row points also_energy to the native adapter.
Native credential test works against api.alsoenergy.com.
Old Cloud Function path is not used for V2 AlsoEnergy.
Production Save/Test is still gated because durable credential storage is not configured.
Required production pieces appear to include:
GOOGLE_APPLICATION_CREDENTIALS_JSON
telemetry_v2_enabled=true
Existing credentials previously entered under in-memory storage should be treated as non-durable and re-entered.
Required Tasks
1. Inspect current durable credential backend implementation

Document the exact code path for telemetry V2 credential-store selection.

Identify:

active backend selector function/class
env vars/settings required
whether GOOGLE_APPLICATION_CREDENTIALS_JSON is supported
whether file-based credentials are still supported
whether gcp_project_id is required
whether telemetry_v2_enabled controls route gating
what causes production Save/Test to return HTTP 503

Return file paths and relevant function/class names.

2. Confirm production env requirements

Without printing secret values, determine the exact production environment variables required.

At minimum, verify whether these are required:

telemetry_v2_enabled
GOOGLE_APPLICATION_CREDENTIALS_JSON
gcp_project_id
Secret Manager project/namespace/prefix setting, if any
credential backend selector setting, if any

Classify each as:

present
missing
invalid/malformed
not required

Do not print values.

3. Configure durable credential storage

If missing, configure production to use durable storage.

Preferred durable backend:

GCP Secret Manager using service account JSON stored in Replit production secrets.

Requirements:

Do not commit secrets.
Do not print secrets.
Do not write service account JSON to repo.
Avoid disk if possible.
If a temp credential file is required, write only to ephemeral storage with restrictive permissions and document it.
Ensure Secret Manager secret names are tenant/provider-account scoped.
Ensure credentials survive redeploy/restart.

If required secrets or IAM roles are not available, stop and report exact operator action needed.

4. Confirm required GCP IAM

Document the least-privilege IAM roles required for the service account.

For Secret Manager, determine whether the app needs:

create secret
add secret version
access secret version
destroy/delete secret
list secrets

Recommend least-privilege roles.

Do not assume broad Owner/Admin roles.

Return:

required service account identity, redacted if needed
required IAM roles
resource scope: project-level or secret-prefix-level, if possible
operator steps if IAM is missing
5. Verify production credential backend after redeploy

After env configuration/redeploy, confirm:

production boot logs show durable telemetry credential backend
production no longer warns/falls back to in-memory
Save/Test routes no longer return HTTP 503 solely due to credential backend
existing in-memory-era credentials are treated as requiring re-entry

Do not use real provider credentials in logs or chat.

6. Run controlled production AlsoEnergy credential test

Do not ask for credentials in chat.

Provide operator steps:

Open production Ilios.
Open AlsoEnergy provider account.
Click Update Credentials.
Re-enter valid AlsoEnergy credentials.
Save.
Click Test Credentials.
Confirm Verified status.

If the operator can perform the UI test during this sprint, record:

status changed to Verified or Invalid
last credential test timestamp updated
last error cleared or shows provider rejection
no GCP/Cloud Function/ConnectionError text appeared

Do not expose credential values.

7. Verify durability across restart/redeploy

After a credential is saved to durable storage:

restart/redeploy the app or simulate backend restart if safe
re-open provider account
click Test Credentials again without re-entering credentials
confirm credentials are still retrievable and testable

If a real credential cannot be used, use a safe fake credential and verify that the same stored fake credential survives restart and produces the same provider rejection category.

Acceptance criteria:

credential fingerprint remains available after restart
test path can retrieve stored credential after restart
no in-memory fallback is used
8. Update runbook

Update docs/RUNBOOK.md §11.d and related telemetry sections with:

exact production env vars required
durable backend verification steps
IAM requirements
how to confirm storage is durable
how to recover if credentials were entered before durable backend
how to rotate provider credentials
what internal users can now test
what remains blocked:
KMC native adapter
Sync Sites customer enablement
external customer onboarding
Acceptance Criteria
Production telemetry V2 credential backend is durable.
Production no longer uses in-memory credential store.
Production Save/Test routes do not return 503 because of credential backend.
AlsoEnergy credentials can be saved and tested in production through the native adapter.
Stored credentials survive restart/redeploy.
Existing in-memory-era credentials are clearly treated as requiring re-entry.
No credentials, tokens, or service account JSON appear in logs/chat.
Runbook updated.
Validation Required

Return:

Files changed
Credential backend before/after
Production env vars required and whether present, redacted
IAM roles required
Production boot/backend verification
Save/Test route behavior after durable backend
Production AlsoEnergy credential test result category
Restart/redeploy durability result
Runbook updates
Remaining blockers
Whether internal users can now test AlsoEnergy credentials in production
Explicitly out of scope
Sync Sites customer enablement
Mapping preservation review
KMC native adapter
QuickBooks
JWT/cookie migration
MFA
Sentry
External customer onboarding