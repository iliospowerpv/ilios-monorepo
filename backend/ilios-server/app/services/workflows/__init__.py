"""Native Workflow Engine service package.

A declarative engine: workflows are DATA (ordered steps that invoke EXISTING endpoints behind
a two-phase preview→confirm→execute), orchestrated by a generic engine. The engine never owns
business truth and creates no parallel mutation layer.
"""
