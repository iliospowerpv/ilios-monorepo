"""Native AI Assistant (Slice 1).

A READ-ONLY orchestration advisor over the native Workflow Engine. It reasons over the same
authorized read-only signals the Workflow Dashboard exposes and answers help/FAQ/product
questions. It can never start, preview, execute, or mutate anything — every governed/operational
action stays exclusive to the human-authorized engine handshake. Isolation: this is wholly
separate from the legacy Due-Diligence chatbot (no shared routes, services, tables, or session).
"""
