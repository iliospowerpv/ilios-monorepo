# API Schema Models

These files represent the input and output of whichever interface this domain logic is imported into.
[Pydantic](https://pydantic-docs.helpmanual.io/) is used to define how data should be in pure python.

**NOTE:** Each logical domain/operation group must be defined in a separate file.
