# CRUD

These files contain the basic DB interfaces described followed the [FastAPI approach to work with SQL databases](https://fastapi.tiangolo.com/tutorial/sql-databases/).

**NOTE:** Each logical domain/entity group must be defined in a separate file.
